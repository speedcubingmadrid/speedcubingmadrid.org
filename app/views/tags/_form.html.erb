
<%= simple_form_for(@tag) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, as: :display %>
    <%= f.input :color, as: :color %>
    <%= f.input :fullname %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
