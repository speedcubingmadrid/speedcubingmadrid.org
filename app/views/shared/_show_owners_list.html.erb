<p>
  <strong>En:</strong>
  <% owners = [] %>
  <% item.owners.each do |owner| %>
    <% owners << "#{owner.user.name} (del #{l(owner.start)} al #{l(owner.end)})" %>
  <% end %>
  <%= owners.join(", ") %>
</p>
