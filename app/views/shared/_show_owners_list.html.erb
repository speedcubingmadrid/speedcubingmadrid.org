<p>
  <strong>En:</strong>
  <% owners = [] %>
  <% item.owners.each do |owner| %>
    <% owners << "#{owner.user.name} (desde el #{l(owner.start)} hasta el #{l(owner.end)})" %>
  <% end %>
  <%= owners.join(", ") %>
</p>
